# 2026-05-04 작업 내역

## 오늘 커밋 요약

| 시간  | 커밋 해시 | 메시지 |
|-------|-----------|--------|
| 12:14 | `2e94f28` | feat: 커뮤니티 카테고리 생성 및 세분화 |
| 19:36 | `e7b3296` | feat: 파일 업로드 추가 |
| 19:45 | `ae544ee` | refactor: 보안절차 확인 |

---

## 커밋 상세

### 1. `2e94f28` — 커뮤니티 카테고리 생성 및 세분화

**변경 파일**: 13개 (1003 추가 / 369 삭제)

#### 커뮤니티 카테고리 시스템 추가
- `community_posts` 테이블에 `category` 컬럼 추가 (기본값: `free`)
- 카테고리별 글 필터링 — 사이드바에서 선택 시 해당 카테고리만 로드
- `getCategoryLabel()` 함수로 카테고리 키 → 한글 라벨 변환
- API: `communityApi.list(token, category, cursor)` — category 파라미터 지원

#### 말씀묵상 페이지 대규모 리뉴얼
- `client/src/app/dashboard/meditation/page.tsx` 전면 재작성 (490줄)
- 말씀묵상 게시글과 커뮤니티 연동 구조 개선

#### 콘티 상세 페이지 수정
- `client/src/app/dashboard/contis/[id]/page.tsx` — 악보 관련 UI 조정 (74줄 변경)

#### 서버 — 커뮤니티 posts
- `posts.service.ts`: `list()` category 필터, `create()` category/title 지원
- `posts.controller.ts`: 카테고리 쿼리 파라미터 처리
- `songs/sheet-music.service.ts`: contiSong 악보 업로드 기능 추가 (41줄)

#### 기타
- `server/tsconfig.json` 경로 설정 수정
- `server/prisma/schema.prisma` — `community_posts` category 컬럼, `conti_songs` sheetMusicUrl 컬럼 추가

---

### 2. `e7b3296` — 파일 업로드 추가

**변경 파일**: 24개 (652 추가 / 221 삭제)

#### 파일 업로드 모듈 신규 생성
- `server/src/uploads/upload.controller.ts` — `POST /api/uploads` 엔드포인트
  - JWT 인증 필수
  - 파일 크기 제한: 20MB
  - 허용 타입: JPEG / PNG / WEBP / GIF / PDF
- `server/src/uploads/upload.service.ts` — Supabase Storage `sheet-music` 버킷에 업로드, public URL 반환
- `server/src/uploads/upload.module.ts` — 모듈 등록
- `client/src/lib/api.ts` — `uploadApi.upload()` 추가

#### 커뮤니티 채팅방 파일 첨부 기능
- `team/page.tsx` — 채팅 입력창에 파일 첨부 버튼 추가
  - 이미지: 인라인 미리보기 (img 태그)
  - PDF: "파일 보기" 링크
  - 첨부 파일 미리보기 패널 (전송 전 확인)

#### 로그아웃 확인 모달
- `components/ConfirmModal.tsx` 신규 생성 — 재사용 가능한 확인/취소 모달
- `components/AppHeader.tsx` — 로그아웃 버튼 클릭 시 ConfirmModal 표시 후 확인 시 로그아웃 처리

#### UI 전반 개선
- `AppHeader.tsx` — 현재 페이지명 표시 (로고 옆 `/페이지명`), 모바일 레이아웃 최적화
- `songs/page.tsx` — 레이아웃 및 검색 UI 개선 (94줄 변경)
- `contis/page.tsx` — 콘티 목록 UI 정리
- `dashboard/page.tsx` — 대시보드 홈 UI 정리
- `login/page.tsx`, `register/page.tsx` — 소소한 UI 수정

#### 서버 가드 추가
- `optional-jwt-auth.guard.ts` — 인증 선택적 가드 (비로그인도 접근 가능하되 유저 정보 있으면 주입)
- `songs.controller.ts`, `songs.service.ts` — 권한 처리 수정
- `teams.controller.ts`, `teams.service.ts` — 소소한 수정
- `bible.controller.ts` — optional guard 적용

---

### 3. `ae544ee` — 보안절차 확인

**변경 파일**: 2개 (140 추가 / 1 삭제)

#### 로그아웃 후 풀 페이지 리로드

- `components/AppHeader.tsx` — `router.replace('/dashboard')` → `window.location.replace('/dashboard')`
  - Next.js 클라이언트 네비게이션은 컴포넌트를 완전히 초기화하지 않아 localStorage가 비워져도 로그인 상태가 잔존할 수 있는 문제 수정
  - `window.location`으로 변경 시 풀 리로드 발생 → 상태 완전 초기화

#### 보안 점검 (코드 수정 없음, 이슈 파악)

다음 항목을 검토하고 추후 수정이 필요한 사항을 식별함:

| 항목 | 현황 | 조치 필요 여부 |
|------|------|----------------|
| 어드민 페이지 URL 접근 | admin 페이지 없음 | 추후 생성 시 middleware + guard 이중 적용 |
| Supabase RLS | service key로 우회 (서버 전용) | Storage bucket public 설정 해제 권장 |
| 환경변수 노출 | .gitignore 적용, 클라이언트엔 NEXT_PUBLIC_API_URL만 | 양호 |
| 결제 금액 검증 | 결제 시스템 없음 | 결제 구현 시 서버 재검증 필수 |
| 에러 메시지 노출 | 3곳에서 내부 에러 상세 노출 | 수정 필요 |
| 이메일 열거 취약점 | resendVerification에서 이메일 존재 여부 노출 | 수정 필요 |
| Rate limiting | 없음 | 수정 필요 |
| Global Exception Filter | 없음 | 수정 필요 |

---

## 누적 기능 현황 (전체)

### 인증
- 이메일/비밀번호 가입 + 이메일 인증 필수
- JWT (accessToken + refreshToken), Redis TTL 관리
- 로그아웃 확인 모달

### 찬양 (Songs)
- CRUD + 검색 (Spotify API 자동완성)
- 구절 기반 찬양 찾기
- 악보 업로드 (Supabase Storage, PDF/이미지)

### 콘티 (Contis)
- 생성/편집/삭제
- conti_songs 순서 관리, Key·템포·송폼 설정
- 악보 인라인 뷰어 (iframe/img)
- PDF 출력, 공유하기

### 팀 (Teams)
- 팀 생성, 초대 링크 (24시간 토큰)
- 멤버 관리 (방장 이전, 멤버 추방)
- 팀 콘티 공유/해제
- 팀 커뮤니티 채팅 (버블 UI, 파일 첨부)

### 커뮤니티
- 카테고리별 게시판 (free / 기타)
- 댓글, 익명 설정
- 이미지/PDF 첨부

### 말씀묵상
- 성경 말씀 JSON 기반 랜덤 추천
- 커뮤니티 연동

### UI/UX
- 시스템 다크모드 자동 적용
- 모바일 최적화
- AppHeader 현재 페이지명 표시
