# 2026-05-05 작업 내역

## 오늘 커밋 요약

| 시간  | 커밋 해시 | 메시지 |
|-------|-----------|--------|
| 12:57 | `d981130` | refactor: 네이버 배포준비 |
| 13:22 | `7a0f3b0` | bug: 사진 첨부 오류 해결 |
| 13:58 | `310798b` | feat: 말씀 묵상 시 묵상나눔으로 이동 |
| 16:25 | `b0ebfb4` | feat: 찬양 키 수정 및 카테고리 생성 |

---

## 커밋 상세

### 1. `d981130` — 네이버 배포준비

**변경 파일**: 3개 (9 추가 / 2 삭제)

- `.gitignore` — 불필요 파일 추가 제외
- `client/public/navere3005bf1d9e73bb76a719ae764cf2bc6.html` — 네이버 웹마스터 도구 소유권 인증 파일 추가
- `client/src/app/layout.tsx` — 소소한 레이아웃 정리

---

### 2. `7a0f3b0` — 사진 첨부 오류 해결

**변경 파일**: 4개 (37 추가 / 5 삭제)

#### 커뮤니티/팀 채팅 파일 업로드 버그 수정

- `community/page.tsx`, `team/page.tsx` — 파일 첨부 시 업로드 API 호출 누락 / 오류 처리 보완
- `server/src/main.ts` — CORS 설정 및 파일 업로드 미들웨어 설정 수정
- `server/src/uploads/upload.controller.ts` — 파일 타입·크기 검증 로직 수정

---

### 3. `310798b` — 말씀 묵상 시 묵상나눔으로 이동

**변경 파일**: 1개 (1 추가 / 1 삭제)

- `client/src/app/dashboard/meditation/page.tsx` — 말씀 묵상 완료 후 커뮤니티 묵상나눔 카테고리(`meditation`)로 자동 이동하도록 라우팅 수정

---

### 4. `b0ebfb4` — 찬양 키 수정 및 카테고리 생성

**변경 파일**: 2개 (247 추가 / 125 삭제)

#### 찬양 키 표기 플랫으로 통일

- `songs/page.tsx`, `contis/[id]/page.tsx` — `KEYS` 배열 샵(#) → 플랫(b) 변환
  - `C#` → `Db`, `D#` → `Eb`, `F#` → `Gb`, `G#` → `Ab`, `A#` → `Bb`
  - 악보 관행상 플랫 표기가 표준이므로 전면 수정

#### 찬양 검색 초성/알파벳 카테고리 필터 추가

- `CAT_SECTIONS` 상수 — 한글(ㄱ~ㅎ) / 영어(A~Z) 섹션으로 구성
- `CHO_TO_CAT` 매핑 — 19개 초성을 11개 대표 자음으로 그룹화 (ㄲ→ㄱ, ㄸ→ㄷ, 쌍자음 포함, ㅋ·ㅌ·ㅍ→ㅊ)
- `getSongCategory()` — 곡명 첫 글자의 초성 또는 영문 알파벳 반환
- 클라이언트 사이드 필터링 (`filteredSongs`) — 텍스트 검색과 독립적으로 중첩 적용 가능

#### 카테고리 사이드바 UI — 커뮤니티 페이지와 동일한 구조로 변경

- `SidebarContent` 컴포넌트 분리 — 섹션 헤더(접기/펼치기) + 항목 리스트
  - 선택된 항목: `bg-violet-50 border-r-2 border-violet-500` (네이버식 오른쪽 인디케이터)
  - 한글 섹션 기본 펼침, 영어 섹션 기본 접힘
- **데스크탑**: `w-52 rounded-xl bg-white border sticky top-20` 흰 카드 사이드바
- **모바일**: 햄버거 버튼 + 슬라이드인 드로어 (커뮤니티 페이지와 동일 패턴)

#### 태그 디자인 교체

- 템포(BPM) 태그: gray → **amber** 계열로 변경
- 성경구절 태그: amber → **gray** 계열로 변경

---

## 추가 작업 (미커밋)

### 5. 콘티 악보 다중 업로드 지원

**배경**: 기존에는 콘티 곡당 악보 1장만 첨부 가능 → 2장 이상 필요한 경우 대응 불가

#### DB 스키마 변경

- `conti_songs.sheet_music_url TEXT` 컬럼 제거
- `conti_song_sheets` 테이블 신규 생성

  ```sql
  CREATE TABLE conti_song_sheets (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    conti_song_id UUID NOT NULL REFERENCES conti_songs(id) ON DELETE CASCADE,
    url           TEXT NOT NULL,
    order_index   INTEGER NOT NULL DEFAULT 0,
    created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW()
  );
  ```

- 기존 `sheet_music_url` 데이터 → `conti_song_sheets`로 마이그레이션 후 컬럼 삭제
- `npx prisma generate` 재실행

#### 서버 — `sheet-music.service.ts`

- `uploadForContiSong()` — 기존 파일 덮어쓰기 방식 → `contiSongSheet` row 추가 방식으로 변경 (업로드마다 누적)
- `removeForContiSong()` 제거 → `removeSheet(sheetId)` 신규 추가 (개별 삭제)

#### 서버 — `contis.controller.ts` / `contis.service.ts`

- DELETE 엔드포인트: `/contis/:id/songs/:contiSongId/sheet` → `/contis/:id/songs/:contiSongId/sheet/:sheetId`
- `CONTI_INCLUDE` 상수에 `sheets: { orderBy: { orderIndex: 'asc' } }` 추가

#### 클라이언트 — `api.ts`

- `ContiSongSheet` 인터페이스 신규 추가 `{ id, url, orderIndex }`
- `ContiSong.sheetMusicUrl` 제거 → `ContiSong.sheets: ContiSongSheet[]`
- `deleteContiSheet()` 시그니처에 `sheetId` 파라미터 추가

#### 클라이언트 — `contis/[id]/page.tsx`

- 카드 뱃지: "악보 보기" → **"악보 N장"** (장 수 표시)
- [+ 악보] 버튼 항상 노출 (추가 업로드)
- "악보 교체" 버튼 제거 (추가 + 개별 삭제로 대체)
- 인라인 뷰어: 모든 장 순서대로 표시, 장별 "삭제" 버튼 개별 제공
- 프린트 레이아웃: `cs.sheets` 배열 순서대로 출력

---

### 6. AppHeader hydration 오류 수정

- `useEffect` + `setUserName` 패턴 → lazy initializer(`useState(() => localStorage...)`) 시도
- SSR에서 빈 문자열, 클라이언트에서 로컬스토리지 값으로 불일치 → hydration mismatch 발생
- **최종 수정**: `useState('')` + `useEffect`로 복원, `// eslint-disable-next-line react-hooks/set-state-in-effect`로 ESLint 경고 suppress

---

### 7. 대시보드 사용 가이드 페이지 추가

- `dashboard/page.tsx` — 말씀묵상 메뉴 아래 "사용 가이드" 링크 카드 추가 (`/dashboard/guide`로 이동)
- `dashboard/guide/page.tsx` 신규 생성
  - 아코디언 방식 (섹션 클릭으로 펼침/접힘)
  - **찬양 관리** 4단계: 추가/검색/악보 업로드/수정·삭제
  - **콘티 작성** 5단계: 생성/찬양 추가·순서 변경/악보 여러 장/PDF 저장/팀 공유
  - **팀스페이스** 4단계: 팀 생성/초대/공유 콘티/커뮤니티 채팅
  - **말씀 묵상** 3단계: 오늘의 말씀/기록/목록
  - 하단 CTA: "찬양 등록하러 가기" 버튼

---

## 추가 작업 (커밋 예정)

### 5. 찬양 관리자 역할(role) 시스템 추가

**서버** — NestJS / Prisma

- `prisma/schema.prisma` — `User` 모델에 `role String @default("user")` 필드 추가
- `npx prisma generate` 재실행으로 타입 반영
- `auth.service.ts` — `generateTokens()`에 `role` 파라미터 추가, login·verifyEmail·refresh 시 JWT payload에 `role` 포함
- `songs.service.ts` — `update()`, `remove()` 시그니처에 `userRole` 추가, `admin`이면 `createdBy` 체크 스킵
- `songs.controller.ts` — `@CurrentUser('role')` 데코레이터로 role 추출, service에 전달
- DB 직접 실행: `ALTER TABLE users ADD COLUMN role TEXT NOT NULL DEFAULT 'user'` + `dlsgh8884@naver.com` → `role = 'admin'` 업데이트

**클라이언트** — `songs/page.tsx`

- `userId`, `userRole` 상태 추가
- useEffect에서 JWT 디코딩 (`atob(token.split('.')[1])`) → `sub`, `role` 파싱
- 수정/삭제 버튼 노출 조건: `token && (userRole === 'admin' || song.createdBy === userId)`
- 토큰 없으면 버튼 미노출, 일반 유저는 본인 곡만, 관리자는 전체

#### 권한 정리

| 역할 | 추가 | 수정/삭제 |
|------|------|-----------|
| 비로그인 | 불가 | 불가 |
| 일반 유저 | 가능 | 본인 곡만 |
| 관리자 | 가능 | 모든 곡 |

---

### 6. 찬양 목록 가나다순 정렬 + 페이지네이션

**서버** — `songs.service.ts`

- `orderBy: { createdAt: 'desc' }` → `orderBy: { title: 'asc' }` 변경 (가나다/ABC 순)

**클라이언트** — `songs/page.tsx`

- `PAGE_SIZE = 20`, `page` 상태 추가
- 검색어 변경·카테고리 변경 시 `setPage(1)` 리셋
- `pagedSongs = filteredSongs.slice((page - 1) * PAGE_SIZE, page * PAGE_SIZE)` 로 그리드 렌더링
- 페이지네이션 UI: 이전/다음 버튼 + 페이지 번호 (현재 ±2 범위 표시, 나머지 `…` 처리)
- 1페이지일 때(totalPages ≤ 1) 페이지네이션 미노출

---

### 7. 찬양 카드 → 커스텀 상세 모달

**클라이언트** — `songs/page.tsx`

- `expandedId` 상태 제거 → `detailSong: Song | null` 상태로 교체
- 카드 인라인 펼침 UI 제거
- 카드 클릭 → `setDetailSong(song)` 으로 모달 오픈
- 카드 디자인 단순화: 제목/아티스트/배지만 표시, hover 시 보라색 테두리
- 상세 모달 구성:
  - 헤더: 제목 + 아티스트 + X 닫기 버튼
  - 배지: 키, BPM, 말씀구절 (말씀 배지 클릭 시 해당 구절 검색 후 모달 닫힘)
  - 가사: 스크롤 가능한 영역 (`overflow-y-auto`)
  - 하단: 수정/삭제 버튼 (권한 있는 경우에만)
  - 배경 클릭으로도 닫기

---

## 누적 기능 현황 (전체)

### 인증

- 이메일/비밀번호 가입 + 이메일 인증 필수
- JWT (accessToken + refreshToken), Redis TTL 관리
- 로그아웃 확인 모달

### 찬양 (Songs)

- CRUD + 검색 (Spotify API 자동완성)
- 구절 기반 찬양 찾기
- 악보 업로드 (Supabase Storage, PDF/이미지)
- **초성/알파벳 카테고리 사이드바 필터** ← 신규
- **키 표기 플랫 통일** ← 신규
- **가나다순 정렬 + 페이지네이션** ← 신규
- **상세 모달** ← 신규
- **관리자 role 시스템** ← 신규

### 콘티 (Contis)

- 생성/편집/삭제
- conti_songs 순서 관리, Key·템포·송폼 설정
- 악보 인라인 뷰어 (iframe/img)
- PDF 출력, 공유하기

### 팀 (Teams)

- 팀 생성, 초대 링크 (24시간 토큰)
- 멤버 관리 (방장 이전, 멤버 추방)
- 팀 콘티 공유/해제
- 팀 커뮤니티 채팅 (버블 UI, 파일 첨부)

### 커뮤니티

- 카테고리별 게시판 (free / 기타)
- 댓글, 익명 설정
- 이미지/PDF 첨부
- **파일 업로드 버그 수정** ← 신규

### 말씀묵상

- 성경 말씀 JSON 기반 랜덤 추천
- **묵상 완료 시 커뮤니티 묵상나눔으로 자동 이동** ← 신규

### UI/UX

- 시스템 다크모드 자동 적용
- 모바일 최적화
- AppHeader 현재 페이지명 표시
- **네이버 웹마스터 소유권 인증** ← 신규
